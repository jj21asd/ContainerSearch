plugins {
    id 'common_process_resources'
}

architectury {
    common platforms.split(',')
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}" // Make @Environment accessible
    compileOnly "com.terraformersmc:modmenu:${project.modmenu_version}"
}
